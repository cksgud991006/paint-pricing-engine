namespace PaintPricingEngine.Measurements;

public class AreaCalculator: IAreaCalculator
{

    public Area CalculateArea(Property property)
    {
        // define business ru,les on calculating area based on room type 
        return Area.Create();
    }
};